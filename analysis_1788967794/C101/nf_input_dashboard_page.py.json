{
  "python_file_path": "pageObjects/web/dashboard_page.py",
  "missing_methods": [
    {
      "intent": "Assert user profile name is visible in dashboard header",
      "proposed_signature": "verify_user_profile_name_visible(expected_name)",
      "detailed_requirements": "Wait for the user profile name element in the dashboard header to be visible. Verify that the displayed profile name matches the expected_name parameter. Return True if visible and matches, otherwise raise an assertion error with a descriptive message.",
      "implementation_checklist": [
        "Locate 'user_profile_name' element using self.page.locator(self.locators['user_profile_name'])",
        "Wait for 'user_profile_name' to be visible using .wait_for(state='visible')",
        "Use expect() to verify 'user_profile_name' is visible with expect(user_profile_name_element).to_be_visible()",
        "Get text content from 'user_profile_name' element using .text_content() or .inner_text()",
        "Assert retrieved text matches expected_name parameter",
        "If assertion passes, return True",
        "If assertion fails, raise AssertionError with descriptive message including expected vs actual values"
      ],
      "required_locator_keys": [
        "user_profile_name"
      ]
    },
    {
      "intent": "Assert user profile name is displayed",
      "proposed_signature": "assert_user_profile_name_displayed(expected_name)",
      "detailed_requirements": "Locate the user profile name element in the dashboard header. Assert that the element is displayed on the page and that its text content matches the expected_name parameter. This method should use explicit waits to ensure the element is present before performing the assertion.",
      "implementation_checklist": [
        "Locate 'user_profile_name' element using self.page.locator(self.locators['user_profile_name'])",
        "Wait for 'user_profile_name' to be visible using .wait_for(state='visible')",
        "Check if 'user_profile_name' is visible using .is_visible()",
        "Assert that .is_visible() returns True",
        "Get text content from 'user_profile_name' element using .text_content() or .inner_text()",
        "Assert retrieved text matches expected_name parameter",
        "If any assertion fails, raise AssertionError with descriptive message"
      ],
      "required_locator_keys": [
        "user_profile_name"
      ]
    }
  ],
  "existing_python_content": "import json\nimport os\nfrom playwright.sync_api import expect\nfrom .common_page import CommonPage\n\ndef load_locator(filename):\n    base_dir = os.path.dirname(os.path.dirname(os.path.dirname(__file__)))\n    locator_path = os.path.join(base_dir, \"locators\", \"web\", filename)\n    with open(locator_path) as f:\n        return json.load(f)\n\nclass DashboardPage(CommonPage):\n    def __init__(self, page, scenario):\n        super().__init__(page, scenario)\n        self.locators = load_locator(\"dashboard_locators.json\")\n\n    def search_product_add_cart(self, product_name):\n        product = self.page.locator(self.locators[\"products\"], has_text=product_name).first\n        product.wait_for(state=\"visible\")\n        add_cart_button = product.locator(\"button\", has_text=\" Add To Cart\")\n        expect(add_cart_button).to_be_visible()\n        if add_cart_button.is_visible():\n            add_cart_button.click()\n\n    def navigate_to_orders(self):\n        self.page.locator(self.locators[\"orders\"]).click()\n        self.page.wait_for_load_state(\"networkidle\")\n\n    def navigate_to_cart(self):\n        self.page.locator(self.locators[\"cart\"]).click()\n        self.page.wait_for_load_state(\"networkidle\")\n        self.page.wait_for_load_state(\"domcontentloaded\")\n"
}